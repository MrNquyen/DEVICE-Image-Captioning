from projects.modules.multimodal_transformer import MultimodalTransformerEncoder, MultimodalTransformerDecoder

def trainer(model, dataloader, config_training):
    
    return model


def eval(model, dataloader):
    return

